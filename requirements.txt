# Robot Navigation System - Python Dependencies

# Core Computer Vision
opencv-contrib-python==4.5.5.64
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# EV3 Robot Control (for deployment on EV3)
python-ev3dev2>=2.1.0  # Alternative: ev3-dc for direct control

# Communication
imagezmq>=1.1.1
jsonpickle>=2.0.0
pyzmq>=22.0.0

# Utilities
rich>=10.0.0
PyYAML>=5.4.0

# Path Planning & Math
heapq  # Built-in, no installation needed
math  # Built-in, no installation needed

# For PC Viewer Only (not needed on EV3)
PyQt5>=5.15.0
Pillow>=8.0.0
imutils>=0.5.4

# Jupyter (for development notebooks)
jupyter>=1.0.0
matplotlib>=3.3.0
ipykernel>=6.0.0

# Optional: For testing and development
pytest>=6.2.0
pytest-cov>=2.12.0

# Note: Install on EV3 (Raspberry Pi/ev3dev):
#   pip3 install opencv-contrib-python==4.5.5.64 numpy scipy
#   pip3 install imagezmq jsonpickle rich PyYAML
#   pip3 install python-ev3dev2
#
# Install on PC (for viewer):
#   pip install -r requirements.txt
#
# For Jupyter notebooks:
#   pip install jupyter matplotlib ipykernel

